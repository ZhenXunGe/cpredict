# ctUSD time-v2 部署与追块验收（2026-09-11）

本记录来自部署机实际执行结果，时间统一说明为上海时间（UTC+8）；机器可读附件中的时间为 UTC。最后只读核验为 **2026-09-11 20:01:40**。它覆盖现有 ctUSD 公开测试站的原地升级、数据库保留、财务激活和 indexer 选择性优化；真实钱包业务全流程仍有未完成项。

完整脱敏数据、镜像摘要、采样序列和原始证据摘要见 [JSON 附件](evidence/ctusd-time-v2-2026-09-11.json)。附件不包含凭据、供应商项目 ID、个人账户地址、部署机主机名或私有绝对路径。原始日志、备份、运行配置继续保存在部署机的 Git 忽略目录。

## 当前结论

- 6 个服务均 healthy，公网 time-v2 已生效；财务快照为 `active` 且 `complete`。
- 20:01:40 已索引与安全链头均为 **307756503**，差 **0 块**。链持续出块，不能把该历史采样当作未来任意时刻的零延迟承诺。
- 更新后的 `npm run stack:update:public -- check` 通过，包括实际运行镜像、公开配置、16 项路由检查和 316 个静态文件的响应大小及 SHA256。
- `/demo` 与 `/demo/` 返回 308，跳转至新站根入口。
- 原 PostgreSQL 容器、镜像、启动时间及数据卷保留。数据库保留核验共 **28 项通过**。
- 16 并发 RPC 小样本无失败；持续追块中记录到 **1 次批次失败，随后恢复**。因此严格的“观察期零失败”条件未通过，不将其描述成全程零错误。

## 提交与运行版本

| 用途 | 实际提交 |
| --- | --- |
| time-v2 合约部署及链上证据 | `83b41967ce78a1edab65b741c9c7965047485c31` |
| 当前 indexer 镜像 | `3a11cc058488938d672c60482afd1b604d7c1f90` |
| 当前 app、metadata、web、preview 镜像 | `b563f0410ee8b1a45c49c0784d3c7346eb6931ba` |
| 实际完成 reconcile/activate 的维护程序 | `053acc3d2e69b97570d935c3978fa866da5c5e4c` |

本证据文档的交付提交是包含本文的后续 `main` 提交。文档提交不会改变上述已运行的镜像来源。合约证据的 `state.source` 没有被改成维护或文档提交。

部署为 Arbitrum Sepolia，chain ID `421614`，协议 `time-v2`：

- Factory：`0xA62eEd107a5aa0924ee0Ba5a915f4B9B832C3913`。
- 部署 ID：`ctusd-421614-a62eed107a5aa0924ee0ba5a915f4b9b832c3913`。
- Manifest hash：`0x69f1869e736ee761b0f4a706eeb68d53e674086b759041b2c8a8dc370f4a4921`。
- 起始块：`307719826`，选择性优化时保持不变。

ctUSD 合约及 6 位小数、Kernel `0.3.1`、EntryPoint `0.7`、派生 index `1001`、原登录与供应商项目均保留。

## 数据库、账户及备份恢复

旧 public schema 在同一 indexer 数据库中隔离归档，保留 **27 个市场、4 个账户、4 个账户主体链接、1 笔 confirmed/finalized 操作**。新 public schema 只绑定 time-v2；旧市场不出现在新部署的公共市场目录中。官方 rollover 重复 apply 返回 `already-applied`。

4 个新部署账户记录与归档记录仅 `deploymentId` 不同，4 个主体链接一致。原代付记录原样结转 1 条，占用 `1000000000000000` wei；历史实际 gas 为 `100683396450693` wei。结转额度与实际 gas 分别保留，没有清除历史预算消耗。运行角色无旧归档 schema 使用权限，结转额度可读而不可增删改，rollover 记录不可改。

期初锚定块为 `307719825`，区块哈希及 4 个账户余额与升级前证据一致。余额整数为 `0、1000000000、0、0`，对应原有 **1000 ctUSD** 保留；没有为旧余额虚构新交易。9 项链上对账检查通过，维护程序在精确核验的快照块 `307732725` 激活财务投影，然后恢复原 indexer 继续追块。

旧市场、交易、持仓、赎回、链上事实、账户与额度核心内容通过备份摘要比对。规则缓存 27 行另从备份 COPY 数据独立比对：仅 6 个 `checked_at` 在在线备份后正常刷新，规则内容一致。原 metadata 与 paymaster 数据库保留，规则发布内容匹配备份。

三个数据库完成导出的只读 MVCC 一致快照备份。每个 dump 与源指纹查询使用同一快照；隔离恢复演练比较恢复前后的记录、投影、检查点与预算，并在增量迁移后再次比较，结果 **PASS**。完成时间为 **18:38:07**。

| 备份 | 字节数 | SHA256 |
| --- | ---: | --- |
| indexer.dump | 162690259 | `466aeb1cb85111cb594730e9799526bb96012fc2dc00a3f18f6be909f18a4c6c` |
| paymaster.dump | 12325 | `48c45819cd1f97a3b1f9ccd263b791deea9d6e8c8c9fbfac370486f8cf308239` |
| metadata.dump | 15482 | `4fe6eb1fbb64a9cb2bd073335df19978062aa58f4294b73385f7ad933f393118` |

原 8 个配置文件和旧运行包有独立核验备份。私有配置保持 0600、公开站点配置保持 0644，均未跟踪到 Git。本次并发优化仅在私有 Compose override 增加 indexer 的并发项，原私有 env、预算和账户配置未变。

## 实际应用预算

已从运行容器挂载配置核对以下整数值，与部署机实际主配置一致。四个功能开关均为 true：`newExposure`、`faucet`、`leaderboard`、`sponsorship`。

| 配置 | 实际 wei | ETH |
| --- | ---: | ---: |
| 单笔 `maxCostPerOperation` | `5000000000000000` | 0.005 |
| 每日新增 project/account/subject **各自** | `80000000000000000` | 0.08 |
| 每日退出 project/account/subject **各自** | `20000000000000000` | 0.02 |
| 每周 `projectWei` | `100000000000000000` | 0.1 |
| 每周 `exitReserveWei` | `20000000000000000` | 0.02 |

周窗口为 `shanghai-monday`，上海周一 00:00 重置。新增和退出的次数限制各自保持项目 200、账户 30、主体 50；`methodDailyOperations=20`。配置中的供应商硬限额仍为 `100000000000000000` wei / `604800` 秒。

完整 `maxGasCost` 预占算法未改；原操作的 `1000000000000000` wei 结转继续计入额度。上述为**已生效的应用配置**。供应商后台当前单笔策略未重新核验，历史认证记录为 `1000000000000000` wei，仍须核对并按授权调整至 `5000000000000000` wei。开启功能开关不构成供应商实际代付验收。

## indexer 选择性升级与并发实测

使用原有 `applySelectedServices` 流程，仅构建、选择 indexer。新镜像摘要：`sha256:8f21e92e996071fc1912f550b4210d15c46b2b22b75bb19583ccf6bcf472e0ec`。其余 5 个服务的容器 ID、镜像和启动时间完全保留；网关只按既有流程平滑重载上游解析。没有 SQL 迁移、修改起始块、回放或再次部署合约。

运行参数为 `CPREDICT_INDEXER_BLOCK_CONCURRENCY=16`，每批 100 块、每 tick 1 批、轮询间隔 1000 ms、确认数 2、RPC 超时 5000 ms。源码默认并发仍为 4，可配置范围为整数 1–32。USDC 运行配置未调整。

升级前使用运行中 indexer 的实际 RPC，以 16 并发、0 重试读取 32 个连续区块头：

| 指标 | 实测 |
| --- | ---: |
| 成功 / 失败 | 32 / 0 |
| HTTP 429 / 限流 / 超时 | 0 / 0 / 0 |
| 延迟中位数 / P95 / 最大值 | 350.36 / 595.84 / 610.58 ms |
| 总耗时 | 966.53 ms |

区块编号和相邻父哈希全部通过，最大在途为 16，所有请求已结束。小样本与后续持续观测分开记录。

19:53:24 切换前差 4308 块；19:53:50 首个切换后采样差 4102 块。**19:59:22 首次追平**，19:59:42 再次差 0 块，20:00:02 的相邻采样差 20 块，20:01:40 再次差 0 块。切换后至首次追平约 332 秒，处理区块速度约 **16.36 块/秒**；这是该窗口的实测值。

持续观察共 16 个采样点，记录到 1 次失败批次，后续提交和 readiness 恢复。现有 `failure(error)` 遥测只累计失败次数，没有保存发生阶段、HTTP 状态或超时分类，日志也没有对应原因。因此无法把该次失败认定为 429 或超时；严格零失败观察结果为未通过。后续区块持续推进，财务覆盖仍完整，健康与追平检查通过。该限制保留在 JSON 附件中。

## 构建、检查和剩余验收

最初 time-v2 镜像构建遇到 Docker inode 耗尽，仅清理未使用构建缓存后，4 个 b563 镜像构建及独立核验通过。对账报告内部 bigint 被带入 JSON 的问题在 `053acc3` 修复；部署机实际执行 4 项针对性测试和后端编译通过，并重跑官方 reconcile/activate 成功。

根任务对 `3a11cc0` 报告 27 项索引/配置/调度/对账测试、34 项 Compose/选择性更新/公开配置测试，以及完整 TS 构建、生成清单和秘密扫描通过。本机另实际完成 indexer Docker 构建、镜像来源核对、唯一环境变更核对、其余服务保留检查、持续追块观测和更新后的标准公网 check。

尚未完成：

- 当前供应商后台单笔策略的认证核验及真实代付证明。
- 真实钱包 Full 创建、零 ETH 控制钱包、新合约资金进出。源码任务报告曾因 MetaMask 延迟签名导致规则挑战过期，需用户配合正确的 Chrome 钱包窗口重新验收；本部署任务未将其记为成功。
- 独立 USDC 与移动实机验收按用户决定后移。

本部署任务未广播链上交易。新操作出现后必须保留新旧 schema 和操作记录，不得用旧数据库备份覆盖新写入。
