# Cpredict 公开用户测试站剩余清单

核对日期：2026-09-10。范围为已确认的公开 ctUSD 用户测试站与独立 USDC 验收环境，不恢复其他旗舰产品专题。

**当前可以交付本地源码，尚不能宣布公开测试产品完成。** B0–B4 的主体代码、B5 的本地工具与部分验收已具备；各批次的实际环境完成条件仍未全部满足。下表区分确定的工程缺口、运行配置和真实验收，不把“已有 SDK 接入”或“本地测试通过”当成真实代付成功。

本清单是本次提交时的待办快照。后续每项关闭时，应追加对应源码版本、环境、证据路径和日期；实测发现的修复也归入对应项。具体命令见 [运行手册](public-test-site-runbook.md)，已完成证据与限制见 [验收记录](public-test-site-acceptance.md)。

## 0. 已完成和已确认，不再重复索取

- 用户站、应用服务、智能账户与代付准入、操作恢复、智能账户规则签名、索引与权益/PnL、创作者/运营/排行榜、迁移/回补/对账工具及独立恢复页面已有本地实现。
- 2026-09-09 的代码验证：334 项单元通过；专用 PostgreSQL 两条通道 21/21、13/13 通过，去重为 30 项；依赖兼容性 5/5；本地浏览器 14/14；新固定区块 Kernel 分叉 21/21。相关构建、契约与生成物检查通过。运行与全量依赖审计当次均为 0 个已知告警。
- 上述浏览器使用测试上下文，Kernel 使用本地资金与 MockUSDC，没有真实移动钱包、托管 Paymaster 或 Circle USDC 的验收证明。严格第三方声明检查另有 35 处失败，不包含在“通过”中。
- Privy/ZeroDev 凭据已经提供；`cpredict-dev` 对应 ctUSD、`cpredict-prod` 对应 USDC，均为 Arbitrum Sepolia。两环境各 9 项只读预检通过，不代表赞助策略生效。
- 每环境每自然周 0.1 ETH，北京时间周一 00:00 重置；新增操作 0.08 ETH、退出专用 0.02 ETH。固定账户 index 分别为 1001、1002，不重新选择。自有 WalletConnect Project ID 可省略，不是当前阻塞。
- 实际公开入口和交易开关仍关闭；旧站、旧部署和既有资产继续保留。私有运行配置、本地报告和工作记录不进入源码提交。

## 1. 仍需工程处理的事项

| ID | 剩余工作 | 关闭条件 |
| --- | --- | --- |
| E01 | 解决锁定 SDK 的 35 处上游 TypeScript 声明错误。Privy 3.41 的隔离发布文件检查仍发现问题，不能直接升级后宣称解决。 | 使用供应商修复或验证过的正式兼容版本，`site:check:dependencies` 通过；不伪造 SDK 类型。若改变依赖，重验账户地址、签名、钱包兼容性与受影响构建。 |
| E02 | 补齐运营端供应商实际花费/余额及策略验证证据。目前 `providerSpendUsd` 固定为未知、`providerPolicyVerified` 固定为 false，页面也明确提示未证明供应商设置。账单导入已实现，但不等于实时剩余额度。 | 接入供应商可验证数据，或明确设计带来源、时间与失效条件的人工核验记录；金额、币种、账期和供应商预算窗口清楚，不能用本地预留金额替代实际花费、直接把验证标记改为 true，或凭 ETH 授权推导美元预算。 |
| E03 | 将新应用服务接入现有运行监控。已有 `/healthz`、数据库 `/readyz`、运营事件和报表中的实时 RPC/链头/索引延迟；尚缺新服务的完整部署探测、采集与告警验证。 | 覆盖数据库、索引/RPC、Privy/ZeroDev 异常、拒绝、未知操作积压、业务与供应商预算；明确采集方式和阈值，模拟故障能够被观察并恢复。不另建外部通知产品。 |
| E04 | 根据实际部署目标补齐用户站和两个环境应用/索引/规则服务的进程或容器编排、私有变量注入和启动顺序。已有构建、迁移与 nginx 模板；现有 Compose 没有完成这套公开站编排。 | 目标环境可以按文档重复启动、重启和升级；六个后端上游、数据库、端口、代理和配置没有混用，原 Demo 仍可用。 |
| E05 | 把已有页面夹具与临时 14 项浏览器检查整理为仓库内可重复运行的回归入口，并接入适用的 CI 检查。属于验收维护工作，当前临时报告不是完整持续回归套件。 | 重复执行真实构建入口与业务夹具检查，保存失败截图/日志；覆盖关键页面、权限、确认/恢复、深链接和两种视口，夹具不进入公开构建。真实钱包仍单独验收。 |
| E06 | 测量 SDK 大包的真实移动网络表现，再处理发现的加载/交互瓶颈。用户站 SDK 与原 Demo 存在大于 500 kB 的提示。 | 在约定设备/网络下记录加载、登录和首笔操作耗时；超出接受标准时按测量结果优化并复测。不能只提高警告阈值。 |
| E07 | 完成依赖分发许可的适用性核验：33 个自定义 LicenseRef、`@metamask/eth-json-rpc-provider@1.0.1` 缺失许可声明。 | 留存正式许可来源和适用结论，缺失条目取得声明或替换；必要时更新 SBOM/notice。现有 SBOM 结构通过不代表许可问题已关闭。 |

## 2. 实际环境配置与必要输入

下列待确认信息影响真实部署和资产归属，不能从旧 sandbox 清单自动推定。已给出的凭据、账户版本和预算不再询问。

| ID | 剩余配置/决定 | 关闭条件与输入来源 |
| --- | --- | --- |
| C01 | 确定 ctUSD 与 USDC 两套协议部署清单及源码关系。 | 提供或确认 manifest 路径、部署 ID/区块、Factory、Marketplace、BondEscrow、FeeVault、支付资产、受益地址、运行时代码 hash 与源码来源。若 USDC 协议尚未部署，先明确目标并准备部署方案，再获得对应部署授权；本次提交不授权广播。 |
| C02 | 确认 USDC 测试资产来源。 | 在实际部署时核对 Circle 公布的 Arbitrum Sepolia 地址、链与 decimals，准备足够的独立测试资金；ctUSD 或 MockUSDC 不能替代此项。 |
| C03 | 确定域名、托管目标、HTTPS 和回调。 | 用户确认实际站点/服务器目标；Privy 允许域名、Google OAuth 回调、移动返回、ZeroDev 策略 HTTPS 地址及恢复入口一一对应。 |
| C04 | 核对 Privy 登录与外部钱包产品配置。 | 邮箱、Google、内嵌钱包、MetaMask/Rabby 检测和移动 WalletConnect 入口在各项目生效；用户不必先注册额外 WalletConnect 项目。 |
| C05 | 配置隔离的数据库、服务环境和缓存。 | 两环境分别用独立 DB/schema、凭据、项目、部署和缓存空间；应用与同环境索引共享规定 schema；TLS、权限、备份位置、变量注入可验证，不向浏览器公开私有配置。 |
| C06 | 配置覆盖所需历史的可靠链读取 RPC。 | 实证目标区间日志、固定区块存储/代码、吞吐与限流。公共 RPC 对旧快照已出现历史状态缺失，换一个新块通过不能关闭旧块重放问题；不把 Bundler 自动当作完整链读取节点。 |
| C07 | 在两供应商项目落实费用硬上限、Custom Policy 和套餐/资金条件。 | 验证每项目硬上限、重置窗口、AND、关闭错误/超时放行、回调认证及业务登记绑定；保留控制台/实际拒绝证据。供应商窗口和应用自然周分别展示，不自动假设重合。原 0.1 ETH 授权不等于购买服务授权。 |
| C08 | 指定只读运营管理员。 | 确认 Privy 登录主体 allowlist；服务端身份核对，普通账户和跨环境主体无法越权。 |
| C09 | 固定首期排行榜的市场与 30 天统计时间。 | 用户确认名单和起止时间；市场开始计榜前发布且不可静默改写；核对创作者及可验证受控账户映射。没有奖励。 |
| C10 | 确认旧站与独立退出的可访问地址。 | 原 Demo/旧 EOA 资产与历史入口可用；恢复页及说明在应用服务中断时可取得，账户版本与资产地址可复核。 |

## 3. 真实登录、账户与设备验收

下列已有实现，但需要在实际配置上实测。桌面/手机视口、临时密钥和只读供应商检查均不能关闭这些项目。

| ID | 待验场景 | 通过标准 |
| --- | --- | --- |
| W01 | 邮箱和 Google 内嵌登录；MetaMask 桌面/移动端、Rabby 桌面；移动 WalletConnect。 | 登录、连接、签名及返回原页成功；按实际验证结果列钱包支持清单。 |
| W02 | 刷新、退出重登、换设备、前端升级、重复开户及多账户切换。 | 同控制钱包/环境/版本稳定对应同一资产地址，控制关系不匹配被拒绝；余额、操作、排行归属独立，无按邮箱或昵称合并资产。 |
| W03 | 深链接、浏览器返回、登录回调、登录后市场截止或价格/余额改变。 | 保留市场和操作上下文，重新读取并由用户确认；旧确认不能直接执行。浏览无需登录。 |
| W04 | 真实 ERC-6492/1271 规则签名，以及 ERC-1155 单笔/批量收发。 | 未部署与已部署应用账户发布规则、接收和转出均成功；错误 challenge、规则哈希、环境和重放被拒绝。 |
| W05 | Privy 官方控制者密钥导出、账户恢复与独立控制。 | 应用和服务器不接触私钥；用户依据已固定账户版本/index 可通过成熟 SDK 控制原资产账户，导出不会被描述成资产自动迁移。 |
| W06 | iOS Safari、Android Chrome、实际移动钱包切换和返回。 | 登录、确认、成交、领取和恢复在实体设备逐项留证；同时验证键盘/焦点、错误读屏标签、菜单、弹窗、小屏布局及浏览器刷新。 |

## 4. 双环境、双钱包的完整业务交易

每条适用流程均要在内嵌与外部钱包上验证；ctUSD 与 Circle 测试 USDC 分别留证。测试资产准备方式不同，USDC 不开放 ctUSD 铸币接口。

| ID | 待验操作 | 通过标准 |
| --- | --- | --- |
| T01 | 控制钱包与应用账户均无 ETH 的首次部署和资产准备。 | 由真实托管 Paymaster 赞助部署；ctUSD 首次领币、每 24 小时 1,000 ctUSD 限额生效；USDC 通过独立来源入账。 |
| T02 | 一级购买及受限授权合并。 | 一次 UserOperation 完成所需授权与购买；支付、最少份额、余额变化、小数/最小单位、错误接收方与授权对象边界符合合约。 |
| T03 | C2C 授权、挂单、部分/全部成交、撤单和终局取回托管份额。 | 份额归属与前后余额正确，卖家实收和双方费用对应事件，所有适用动作有真实代付证据。 |
| T04 | 创建、结算、各类作废、空市场，以及创作者押金和费用领取。 | 创建费/押金与网络 Gas 分开；规则、权限、押金锁定/记账/罚没/领取和聚合费用领取符合链上状态。 |
| T05 | 赢家领取、零持仓早鸟、退款、超时退本金及分阶段补偿。 | 所有权益可发现并在正确条件下操作；金额和消耗份额正确，不漏原始买家或托管权益。 |
| T06 | 应用账户向用户明确确认的地址转出当前环境支付资产。 | 赞助范围、接收方、资产/数量确认与实际到账一致，错误环境/地址不被执行。 |
| T07 | 应用服务中断、供应商中断后的独立退出。 | 使用导出控制者/外部钱包、固定账户配置及独立 RPC/Bundler 取回可退出资产；缺 ETH 时明确费用并重新确认，自付后到账。不能只展示恢复表单。 |
| T08 | 费用、预算与操作确认。 | 项目 Gas、协议费、押金分别展示；每笔有明确用户确认；预算不足优先保留退出，无自动扣 USDC 或无提示转成自付，也无长期任意交易会话权限。 |

## 5. 恢复、防滥用与页面异常

单元/数据库已有相应覆盖；以下仍须供应商、实际部署和浏览器组合验证。

| ID | 待验场景 | 通过标准 |
| --- | --- | --- |
| R01 | 双击、跨标签页、刷新、跨设备重复操作。 | 同一动作指向同一持久化记录；只发送一次，恢复不创建新的资金操作。 |
| R02 | 供应商已接收但响应丢失、RPC 超时、回执延迟、结果未知。 | 保存业务/供应商 ID、UserOperation/交易 hash；只查原操作，不自动重发。 |
| R03 | 签名拒绝、执行回滚、交易替换、bundle 成功但 UserOperation 失败、确认后重组。 | 准备/待签/提交/确认/成功/回滚/取消/未知准确区分；按原 hash 恢复，失败不记成成功到账。 |
| R04 | 成功后余额刷新失败、旧账户或旧环境请求晚到、索引落后。 | 不改写链上成功，不显示旧账户余额或允许旧确认；同步延迟与操作结果分别显示。 |
| R05 | 规则不可验证、市场截止、无历史、空池、新增交易/领币/排行榜分别关闭。 | 不产生新增资金暴露，不编造赔率/收益；退出、已有资产、历史和原操作查询仍可访问。 |
| R06 | 绕过前端、伪造 sender/projectId/账户、任意合约/方法、模块/升级、nonce/金额/接收方/授权对象篡改。 | 实际内层调用与认证登记逐项绑定；任一不一致拒绝代付，供应商错误或超时不放行。 |
| R07 | 单账户、登录主体、方法、项目额度，超额领币，周一重置及跨周未决预留。 | 真实回调中限额同时生效；每环境独立，新增交易不能动用 0.02 ETH 退出额度，供应商硬上限确实拒绝超额请求。 |
| R08 | 用户查询他人操作、后台越权、跨环境缓存/数据库污染、伪造代理头。 | 服务端拒绝越权；私有令牌、签名、失败操作原文不公开；真实反向代理下限流身份正确。 |
| R09 | 服务重启和数据库恢复后继续查询；浏览器/服务日志及产物敏感信息。 | 已提交/未知记录保持同一身份与 hash；没有私钥、访问令牌或可执行签名泄露，公网不能读取私有配置。 |

## 6. 实际历史、权益和金额对账

| ID | 剩余工作 | 关闭条件 |
| --- | --- | --- |
| D01 | 实际库执行应用 001 与索引器 006 增量迁移、影子投影重放和切换预演。 | 迁移可重复、校验和一致，原始数据和旧读取路径保留；达到同一代码/epoch/区块/hash 且对账通过后才激活。 |
| D02 | 从实际部署区块盘点原始事件覆盖，补齐明确缺口与已验证账户资产流。 | 市场发现与合约集合完整；只对已验证账户定向补支付资产/EntryPoint 事件，不全链扫描 USDC；重复回放不重复入账，缺口未补时不能标完整。 |
| D03 | 在同一确认区块做链上全额对账。 | 余额、物理持仓、托管、totalSupply、早鸟分数/池余额、押金/费用、退款和领取金额与链上对应；ctUSD/USDC 分别通过并保存证据。 |
| D04 | 用真实成交和终局事件核对成本/PnL。 | 移动平均成本、部分成交、卖家净收入、费用不重复扣、买入与铸造不重复记成本、托管不实现收益、折价买入退款、败方终局只记一次损失均正确；充值/铸币/转出/创作者收入/项目 Gas 不混入交易者收益。 |
| D05 | 核对完整权益并集与未知成本。 | 当前持有人、原始买家、挂单卖家、押金/费用受益人均可发现；零持仓早鸟和分阶段超时权益不遗漏；外部转入成本未知时不按零成本计算或入榜，外部转出不当亏损。 |
| D06 | 完整历史分页与筛选。 | 时间/市场/类型筛选、服务端游标、共同查询快照、区块/交易序号/日志序号顺序正确；无事件推进、重复日志、部分回补不会隐瞒缺口，重组使旧游标明确失效。 |
| D07 | 实际规模下的回放与重组更正。 | 重组撤销相应账本/排名贡献并保留更正和操作日志；晚到事件可重算；超过既有事实量/检查数上限显式停止，按实测扩容，不截断数据假装通过。 |

## 7. 排行榜、创作者与运营报表

| ID | 待验事项 | 通过标准 |
| --- | --- | --- |
| O01 | 首期市场与受控账户资格、30 天窗口、并列分页。 | 非指定市场不计榜；创作者及验证受控账户不参与自己的市场；成本不完整的账户对应期间不入榜；并列稳定，ctUSD 不标为美元，无奖励/投资能力暗示。 |
| O02 | 共同统计区块、快照版本及更正。 | 全部相关数据到同一区块才发布；期末时间正确；晚到事件/重组保留旧版本与更正原因，不静默改榜。 |
| O03 | 创作者和协议收入。 | 应计、当前可领取、实际领取分别可追溯；押金锁定/记入余额/罚没/实际领取分开，跨市场聚合到账不伪装成某一市场的领取。 |
| O04 | 漏斗、活跃和日报口径。 | 访问会话、登录主体、资产账户和全部地址分别统计；首次交易/领取/复投基于真实成功事件；上海时间半开窗口、事件时间和查询时余额快照说明正确。 |
| O05 | 单 UserOperation Gas、供应商原始账单与实时预算。 | 每个操作成本来自对应事件，不重复分摊 bundle Gas；真实账单按原账期/币种与唯一编号导入并对账；实际供应商支出和本地额度的差别可见，结合 E02/C07 关闭。 |
| O06 | 后台鉴权、反馈保存和服务异常展示。 | 两环境管理员权限与数据隔离实测通过；反馈可按编号追溯且有明确处理人/读取方式，不要求新增邮件通知；服务错误/代付拒绝/未知操作与真实故障记录一致。 |

## 8. 公开发布与运行验收

| ID | 剩余工作 | 关闭条件 |
| --- | --- | --- |
| P01 | 根据实际目标流量做容量测试和运行阈值设置。 | 覆盖登录/查询/代付并发、DB 连接与慢查询、RPC 限流、索引追赶、操作恢复积压；给出接受标准和测量值，不能用空库健康检查替代。 |
| P02 | 渲染并验证目标 nginx、HTTPS、安全头、CSP 与静态缓存。 | Privy/OAuth/WalletConnect 必需域名经过实际验证；路由刷新、恢复页、旧 Demo 跳转正确；site-config 不缓存，内部端口/metrics/readyz 不公开，可信代理范围准确。 |
| P03 | 实际备份、恢复、迁移回退与服务中断演练。 | 在受控目标恢复数据库和配置，验证余额、历史与操作记录，服务重启后查询原操作；前端/读取版本可回退，不清数据、不重发未知资金操作。 |
| P04 | 监控与故障演练。 | 部署后的健康/就绪、索引/RPC/供应商、预算与积压监控有实际证据；故障可被发现并按手册恢复，结合 E03 关闭。 |
| P05 | 归档最终版本的分层验收证据。 | 源码提交、构建摘要、脱敏配置/部署、数据库迁移、真实钱包/设备、ctUSD/USDC 前后余额、操作 ID/hash/区块、容量及恢复报告互相绑定。代码或配置发生影响性变化时复验对应项；本地忽略目录的报告需按发布流程脱敏归档。 |
| P06 | 获得明确目标的发布授权，再执行有限流量观察和开放。 | 完成所有发布阻塞项及实际验收后，用户批准目标与发布；检查域名、服务、首批真实操作和预算，再逐步开放。发生异常按 P03 回退并保留查询。此次“提交”仅授权本地 Git 提交。 |

## 9. 下一步顺序与范围边界

1. 优先确认 C01/C03/C07：两份实际部署清单、站点目标与供应商硬上限/策略。并行准备 E01/E02/E03/E05 及许可证据；无需重复注册已提供的项目。
2. 完成隔离环境与运行编排，验证可靠 RPC、迁移/回补/同区块对账，再开展 W/T/R 的真实钱包和设备联调。
3. 用实际账本关闭 D/O，完成容量、移动性能、监控、备份恢复、HTTPS 和最终版本证据。
4. 取得具体发布目标授权，按 P06 开放。未完成项保持明确状态，不把“源码已提交”称为“站点已上线”。

本次计划外：主网真实资金、法币通道、跨链桥、奖励发放、OBS 工具、外部通知系统、完整信誉评分及其他原有旗舰产品专题。它们不计入本次剩余清单；新增时另定范围与验收。
